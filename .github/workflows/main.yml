name: Maven CI
 
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
 
jobs:
  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
 
    
        
    - name: Deployment
      uses: cross-the-world/ssh-scp-ssh-pipelines@v1.1.4
      with:
        host: 18.208.163.244
        port: 22
        user: ubuntu
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        last_ssh: | 
                curl ifconfig.me
                sudo aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 323922828731.dkr.ecr.us-east-1.amazonaws.com
                sudo docker pull 323922828731.dkr.ecr.us-east-1.amazonaws.com/apache-registry:latest
                sudo docker run -itd -p 80:80 323922828731.dkr.ecr.us-east-1.amazonaws.com/apache-registry 
